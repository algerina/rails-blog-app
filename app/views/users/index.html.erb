<h1 class= "title">The Light Box</h1>
    <main class= "main">
    <ul>
    <% @users.each do |user| %>
        <li class="grow">
    <%
=begin%>
 <div><%= image_tag (user.photo), height: 200 ,width: 200 %></div> 
<%
=end%>
<%= image_tag (user.photo) unless user.photo.nil? %>
    <button class="counter"><%= link_to (user.name),user_path(user.id) %></button>
            <h2>Number of posts: <%= user.posts_counter%></h2>
            <%end%>
            </li>
    </ul>
    
    </main>


